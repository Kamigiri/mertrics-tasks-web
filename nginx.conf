server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Include mime types
    include /etc/nginx/mime.types;

    # Serve static files from /mertrics-tasks/
    location /mertrics-tasks/ {
        try_files $uri $uri/ /mertrics-tasks/index.html;
    }

    # Redirect root to /mertrics-tasks/
    location = / {
        return 302 /mertrics-tasks/;
    }

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
